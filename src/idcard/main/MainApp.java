package idcard.main;

import idcard.ui.LoginView;
import javafx.application.Application;

public class MainApp {
    public static void main(String[] args) {
        Application.launch(LoginView.class, args);
    }
}